# CLAUDE.md — контекст проекта SEBOSTIAN

Этот файл читает ИИ-ассистент (Claude и др.) перед работой с репозиторием.
Общее описание для людей — в `README.md`. Здесь — всё, что нужно, чтобы не наступать на старые грабли.

## Кто и как работает

- Автор — Виталий, студент IT, помощник сисадмина. Пишет по-русски, неформально.
- Отвечать **коротко и по делу**, по-русски; термины — с кратким пояснением в скобках.
- Больше схем и картинок (SVG), меньше простыней текста. Схемы — по пинам: «нога резистора 1 → куда, нога 2 → куда».
- Отдавать **готовые файлы целиком** (main.cpp, platformio.ini), а не куски. Если меняется только одна строка в конфиге — сказать какую, а не присылать файл целиком (иначе затрётся IP в `upload_port`).
- «Не глупи»: думать до конца (пример: стерео с общим мьютом требует **2 транзистора**, по одному на каждый SD, а не один).
- Не выдавать догадки за факты. Если что-то не проверено на железе — так и сказать.

## Проект

Умная колонка «Себастьян» для локального голосового ИИ-дворецкого (сервер Себастьяна: Ollama + Whisper + XTTS + MCP на Ubuntu VM, Proxmox).
Сейчас: **тестовый стенд**, все узлы проверены, стоит тестовая прошивка с экранами.
Дальше: голосовой контур — прошивка XiaoZhi на S3 ↔ сервер Себастьяна (у XiaoZhi свой WebSocket-протокол, у сервера HTTP+SSE — нужен адаптер на сервере).

## Структура репо

- `S3/` — голова, ESP32-S3-N16R8 (16 МБ флеш, 8 МБ octal PSRAM). PlatformIO.
- `ESP32/` — со-процессор, ESP32 D0WD-V3 (4 МБ, без PSRAM, мост CH340). PlatformIO.
- `src/secrets.h`, `secrets.ini` — пароли, **в .gitignore**. Никогда не коммитить и не выводить их содержимое.

## Сборка — зафиксировано, не менять без причины

- `platform = espressif32@6.5.0` (Arduino core 2.0.14). На core 3.x TFT_eSPI падает в init.
- Драйвер I2S — **legacy** `driver/i2s.h`.
- Git на ПК автора нет → в `lib_deps` git-ссылки не работают, только архивы (`.../archive/refs/tags/...zip`) или реестр.
- Голова: `board_build.partitions = default_16MB.csv` **и** `board_upload.flash_size = 16MB` (без второго — бесконечная перезагрузка загрузчика). `memory_type = qio_opi`, `ARDUINO_USB_CDC_ON_BOOT=0` (монитор на UART-порту, COM4).
- Со-процессор: `min_spiffs.csv` (2 слота по 1.9 МБ, прошивка ~1.55 МБ). `huge_app.csv` нельзя — нет OTA.
- `[platformio] extra_configs = secrets.ini`, `default_envs` = кабельное окружение (иначе Upload шьёт все env подряд).
- Окружения: `esp32s3` / `esp32s3_ota`, `esp32` / `esp32_ota` (espota, пароль из secrets.ini).
- TFT_eSPI настраивается через build_flags (ST7789, BGR, INVERSION_OFF, шрифты GLCD/2/4/7, SPI 27 МГц).

## Прошивка по Wi-Fi (OTA)

- Голова: Wi-Fi + ArduinoOTA стартуют сами (hostname `sebastian-head`), NTP-часы `MSK-3`.
- Со-процессор: по команде `OTA` от головы → `a2dp.end(true)` (BT выкл + освобождение памяти) → Wi-Fi → ArduinoOTA (`sebastian-coproc`) → 5 мин таймаут → `ESP.restart()`. BT и Wi-Fi одновременно на ESP32 = заикания звука, поэтому так.
- Если новая прошивка падает до старта Wi-Fi — OTA мёртв, нужен кабель. Любые изменения старта (setup) делать осторожно.

## Железо и пины

Голова (S3):
- Дисплей ST7789 240×320 (не ILI9341, хоть плата «ILI»), rotation 2 (перевёрнут). Тач XPT2046 на той же SPI.
  SCK 12, MOSI 11, MISO 13, LCD_CS 10, DC 9, RST 14, BL 21, TOUCH_CS 16, T_IRQ 17.
- SD: своя шина HSPI — SCK 6, MOSI 7, MISO 15, CS 5, 10 МГц, только FAT32.
- Динамики: 2× MAX98357A на **I2S1** через свой класс `I2SOut` (библиотечный AudioOutputI2S на порту 0 на S3 не играл). BCLK 40, LRC 41, DIN 42. Порядок сэмплов стандартный {L, R}.
- Микрофон INMP441 на **I2S0**: SCK 47, WS 48, SD **38**, 32 бит, ONLY_LEFT, `raw >> 14`.
- Мьют динамиков: GPIO2 → 2× BC547 (база через 1к, 10к на землю, коллектор → SD своего MAX). LOW = играют.
- XSMT PCM5102A: GPIO39, HIGH = AUX играет. Детект штекера: GPIO3 (пин 4 гнезда через 10к), HIGH = вставлен. AUX включается только если штекер вставлен и не нажат мьют.
- UART к со-процессору: Serial1 RX 18, TX 8, 115200.
- Запрещено: 35–37 (PSRAM! микрофон на 37 давал TG1WDT-перезагрузки), 43/44 (монитор), 19/20 (USB), 0/45/46 (strapping). Свободно: 1, 4.
- `heap_caps_malloc_extmem_enable(64*1024)` — мелкие буферы во внутренней RAM.

Со-процессор (ESP32):
- A2DP-приёмник «Sebastian» (ESP32-A2DP v.1.7.5) → I2S0 → PCM5102A: BCK 26, LCK 25, DIN 22.
- ARGB (4 диода): GPIO32 → 470 Ом → Din (начало ленты). 74AHCT125 не нужен.
- TTP223: 36/35/34 (T1 тише, T2 play/stop, T3 громче). Кнопка мика: 4 (pull-up).
- Батарея: GPIO33, делитель 1:2 от B+ (плюс держателя), `analogReadMilliVolts ×2`.
- Радио 433 (RCSwitch): RX 27 (через делитель 10к/20к, RX питается 5 В), TX 13. Протокол 1, импульс 389 мкс, 24 бита, повтор 8.
  Триггеры (приём): 1382424, 1382420, 1382428, 1382418, 1382426, 1382422, 1382423, 1382431 (антидребезг 1 с).
  Подсветка ПК (передача): 9348096 + n, n = 1..21. HOLD = код #1 три раза подряд (пачка 2.5–5 с НЕ работает — проверено).
- 74HC4053 (выбор источника: S3 или BT на усилители/PCM) — GPIO21, LOW = S3, +10к на GND. Ещё не установлен.
- UART к голове: Serial2 RX 16, TX 17. Никогда не TX0/RX0 (USB-мост).
- Запрещено: 12 (strapping, HIGH = не грузится), 0/2, 1/3. Свободно: 14, 18, 19, 23, 39 (вход).

Питание: UPS-модуль HW-465 (2×18650 параллельно, USB-C, boost 5 В 3 А, авто-переход) → тумблер → рельс 5 В. Перемычки 9V/12V не паять (MAX98357A max 5.5 В). Модуль ест ~25 мА постоянно. Общая земля обязательна.

## Проверенные на железе выводы (важно!)

- **MAX98357A (клоны):** SD подтянут к VIN через **~4 кОм** на плате, внутреннего pull-down почти нет. Пороги SD: <0.16 В выкл, 0.16–0.77 микс, 0.77–1.4 правый, >1.4 левый. Левый = ничего не паять. Правый = **1 кОм SD → GND** (1.2 кОм при питании от голой батареи). Резисторы на 5 В (100к, 680к) бесполезны.
- **PCM5102A:** перемычки снизу — H1 (FLT), H2 (DEMP), H4 (FMT): середина → L. Без FMT → шум вместо звука. SCK → GND.
- **Гнездо AUX (5 пинов):** 1 = R, 5 = L, 3 = GND, 4 = детект L (касается 5 без штекера), 2 = детект R (не используется).
- **Тач + дисплей на одной SPI:** после каждого `tft.getTouch()` — «жертвенная» запись (`drawPixel`), тач читать только по T_IRQ. Калибровка `{460, 3320, 340, 3430, 1}` снята в rotation 0 → при rotation 2 зеркалить `x = 239 - x`, `y = 319 - y`.
- Один MAX98357A сдох при перепайках на живую — перепаивать только без питания.
- Микрофон тише музыки → в тесте ФВЧ 120 Гц + усиление по RMS + tanh-лимитер.

## Тестовая прошивка (текущее состояние)

- Голова v13: 5 экранов (HOME с часами, AUDIO, SENSORS, RADIO, SYSTEM), листание < >. Музыка при старте не играет.
- Со-процессор v6.1: BT-колонка, радуга на ленте при старте, радио, сенсоры, батарея, OTA-режим.
- Протокол UART — таблица в README. Новые команды добавлять туда же.
- Версию писать в шапке `main.cpp` и в строке старта (`SEBASTIAN BENCH vN`), вести краткую историю версий в комментарии.
- Шрифт экрана без кириллицы → надписи на экране латиницей/транслитом.

## TODO

- 74HC4053 (ждём), замена MAX98357A, питание от UPS, корпус.
- Голосовой контур: XiaoZhi ↔ сервер Себастьяна.
- Возможно: разъём/ESP-NOW для внешней ESP (свободные пины со-процессора: UART1 на 18/19).
